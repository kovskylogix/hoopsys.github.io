import { KlorderbydatePipe } from './klorderbydate.pipe';

describe('KlorderbydatePipe', () => {
  it('create an instance', () => {
    const pipe = new KlorderbydatePipe();
    expect(pipe).toBeTruthy();
  });
});

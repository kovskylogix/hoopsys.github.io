import { KlfilteridPipe } from './klfilterid.pipe';

describe('KlfilteridPipe', () => {
  it('create an instance', () => {
    const pipe = new KlfilteridPipe();
    expect(pipe).toBeTruthy();
  });
});

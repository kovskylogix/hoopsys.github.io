import { CustomfilterbypatientidPipe } from './customfilterbypatientid.pipe';

describe('CustomfilterbypatientidPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomfilterbypatientidPipe();
    expect(pipe).toBeTruthy();
  });
});
